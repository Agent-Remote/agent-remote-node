name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: Release version without the leading v
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: actions/setup-go@v6.5.0
        with:
          go-version-file: go.mod
      - name: Resolve release version
        id: version
        env:
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          version="${INPUT_VERSION:-${GITHUB_REF_NAME#v}}"
          version="${version#v}"
          if ! [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+([-.+][0-9A-Za-z.-]+)?$ ]]; then
            echo "invalid semantic version: $version" >&2
            exit 2
          fi
          test "$GITHUB_REF" = "refs/tags/v${version}"
          source_version=$(sed -n 's/^var DefaultVersion = "\([^"]*\)"/\1/p' internal/config/config.go)
          test "$source_version" = "$version"
          echo "value=${version}" >> "$GITHUB_OUTPUT"
      - name: Resolve pinned device proxy dependency
        id: device
        run: |
          set -euo pipefail
          test "$(jq -er .schema_version release-dependencies.json)" = "2"
          test "$(jq -er .device_proxy.repository release-dependencies.json)" = \
            "Agent-Remote/agent-remote-device"
          version=$(jq -er .device_proxy.version release-dependencies.json)
          commit=$(jq -er .device_proxy.commit release-dependencies.json)
          workflow=$(jq -er .device_proxy.release_workflow release-dependencies.json)
          [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+([-.+][0-9A-Za-z.-]+)?$ ]]
          [[ "$commit" =~ ^[0-9a-f]{40}$ ]]
          [[ "$workflow" =~ ^[A-Za-z0-9][A-Za-z0-9._-]*\.ya?ml$ ]]
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "commit=$commit" >> "$GITHUB_OUTPUT"
          echo "workflow=$workflow" >> "$GITHUB_OUTPUT"
      - name: Scan reachable Go vulnerabilities
        env:
          VERSION: ${{ steps.version.outputs.value }}
        run: |
          set -euo pipefail
          report="agent-remote-node-${VERSION}.govulncheck.json"
          go run golang.org/x/vuln/cmd/govulncheck@v1.6.0 -json ./... > "$report"
          sha256sum "$report" > "$report.sha256"
      - uses: actions/checkout@v7.0.0
        with:
          repository: Agent-Remote/agent-remote-device
          ref: v${{ steps.device.outputs.version }}
          path: .external/agent-remote-device
          persist-credentials: false
      - name: Verify managed skill matches tagged device source
        env:
          DEVICE_COMMIT: ${{ steps.device.outputs.commit }}
        run: |
          test "$(git -C .external/agent-remote-device rev-parse HEAD)" = "$DEVICE_COMMIT"
          scripts/check-managed-skills.sh .external/agent-remote-device
      - uses: sigstore/cosign-installer@v4.0.0
      - name: Download and verify matching device proxy releases
        env:
          GH_TOKEN: ${{ github.token }}
          DEVICE_VERSION: ${{ steps.device.outputs.version }}
          DEVICE_WORKFLOW: ${{ steps.device.outputs.workflow }}
        run: |
          set -euo pipefail
          download="$RUNNER_TEMP/device-download"
          proxy_root="$RUNNER_TEMP/device-proxies"
          mkdir -p "$download" "$proxy_root"
          gh release download "v${DEVICE_VERSION}" \
            --repo Agent-Remote/agent-remote-device \
            --dir "$download" \
            --pattern "agent-remote-device-proxy-*-${DEVICE_VERSION}.tar.gz" \
            --pattern "agent-remote-device-proxy-*-${DEVICE_VERSION}.tar.gz.sha256" \
            --pattern "agent-remote-device-proxy-*-${DEVICE_VERSION}.tar.gz.sigstore.json"
          for label in linux-amd64-glibc linux-arm64-glibc linux-amd64-musl linux-arm64-musl; do
            archive="agent-remote-device-proxy-${label}-${DEVICE_VERSION}.tar.gz"
            test -f "$download/$archive"
            (cd "$download" && sha256sum --check "$archive.sha256")
            cosign verify-blob \
              --bundle "$download/$archive.sigstore.json" \
              --certificate-identity \
                "https://github.com/Agent-Remote/agent-remote-device/.github/workflows/${DEVICE_WORKFLOW}@refs/tags/v${DEVICE_VERSION}" \
              --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
              "$download/$archive"
            mkdir -p "$proxy_root/$label"
            tar -xzf "$download/$archive" -C "$proxy_root/$label"
          done
          echo "DEVICE_PROXY_DIR=$proxy_root" >> "$GITHUB_ENV"
      - name: Run tests
        run: |
          go test ./...
          sudo env \
            "PATH=$PATH" \
            "HOME=$HOME" \
            "GOCACHE=$RUNNER_TEMP/root-go-cache" \
            AGENT_REMOTE_RUN_NETNS_TEST=1 \
            go test -run '^TestDialNetworkNamespaceLoopbackIntegration$' ./internal/runtimehelper
          tests/install_scripts_test.sh
      - name: Build release packages
        env:
          VERSION: ${{ steps.version.outputs.value }}
        run: scripts/build-release.sh
      - name: Generate release checksums
        run: |
          set -euo pipefail
          for archive in dist/*.tar.gz; do
            archive_name=$(basename "$archive")
            (cd dist && sha256sum "$archive_name" > "$archive_name.sha256")
          done
      - name: Generate Node release SBOM
        uses: anchore/sbom-action@v0
        with:
          path: dist
          format: spdx-json
          output-file: dist/agent-remote-node-${{ steps.version.outputs.value }}.spdx.json
      - name: Sign Node release archives
        env:
          VERSION: ${{ steps.version.outputs.value }}
        run: |
          set -euo pipefail
          for archive in dist/*.tar.gz; do
            cosign sign-blob --yes --bundle "$archive.sigstore.json" "$archive"
          done
          report="agent-remote-node-${VERSION}.govulncheck.json"
          cosign sign-blob --yes --bundle "$report.sigstore.json" "$report"
          sbom="dist/agent-remote-node-${VERSION}.spdx.json"
          cosign sign-blob --yes --bundle "$sbom.sigstore.json" "$sbom"
      - name: Attest Node release archives
        uses: actions/attest-build-provenance@v3
        with:
          subject-path: dist/*.tar.gz
      - name: Verify Node release evidence
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.value }}
        run: |
          set -euo pipefail
          jq -e '.spdxVersion | startswith("SPDX-")' \
            "dist/agent-remote-node-${{ steps.version.outputs.value }}.spdx.json" >/dev/null
          sbom="dist/agent-remote-node-${VERSION}.spdx.json"
          cosign verify-blob \
            --bundle "$sbom.sigstore.json" \
            --certificate-identity \
              "https://github.com/${GITHUB_REPOSITORY}/.github/workflows/release.yml@${GITHUB_REF}" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            "$sbom"
          for archive in dist/*.tar.gz; do
            (cd dist && sha256sum --check "$(basename "$archive").sha256")
            cosign verify-blob \
              --bundle "$archive.sigstore.json" \
              --certificate-identity \
                "https://github.com/${GITHUB_REPOSITORY}/.github/workflows/release.yml@${GITHUB_REF}" \
              --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
              "$archive"
            gh attestation verify "$archive" --repo "$GITHUB_REPOSITORY"
          done
          report="agent-remote-node-${VERSION}.govulncheck.json"
          sha256sum --check "$report.sha256"
          cosign verify-blob \
            --bundle "$report.sigstore.json" \
            --certificate-identity \
              "https://github.com/${GITHUB_REPOSITORY}/.github/workflows/release.yml@${GITHUB_REF}" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            "$report"
      - uses: softprops/action-gh-release@v3.0.1
        with:
          tag_name: v${{ steps.version.outputs.value }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            dist/*.tar.gz
            dist/*.tar.gz.sha256
            dist/*.tar.gz.sigstore.json
            dist/*.spdx.json
            dist/*.spdx.json.sigstore.json
            agent-remote-node-${{ steps.version.outputs.value }}.govulncheck.json
            agent-remote-node-${{ steps.version.outputs.value }}.govulncheck.json.sha256
            agent-remote-node-${{ steps.version.outputs.value }}.govulncheck.json.sigstore.json
